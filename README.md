# BottTheWoz
reddit/twitter bot for the moderators of /r/scottthewoz

* Requires `tweepy` and `praw` python packages
* Place `secrets.py` in the same directory as `bottthewoz.py`
* Run using `python3 bottthewoz.py`
